import React from 'react';
import fakeData from '../../fakeData/products.json';

const Shop = () => {
        console.log(fakeData);
    return (
        <div>
            <h1>
                This is shop
            </h1>
        </div>
    );
};

export default Shop;